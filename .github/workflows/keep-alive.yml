name: Keep Render Alive

on:
  schedule:
    # Run every 9 minutes
    - cron: '*/9 * * * *'
  workflow_dispatch: # Allows manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: |
          curl -X GET https://newsscope-backend.onrender.com/health
